{"config":{"lang":["en"],"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"","level":1,"title":"FinAgent","text":"<p>FinAgent is a local-first personal finance agent. It imports your bank and credit-card statements, categorizes every transaction with rules, location lookup, and a local LLM, and renders everything in a dashboard that runs on your own machine.</p> <p>Nothing leaves your computer. No cloud accounts, no telemetry, no data uploads.</p>","path":["FinAgent"],"tags":[]},{"location":"#where-to-go","level":2,"title":"Where to go","text":"Topic Page Install the app (DMG or binary) Installation What happens the first time you launch First-run setup Getting statements into FinAgent Importing statements How processing works, progress, and dates Processing pipeline Every terminal flag and utility Running from the terminal Stop / restart the app cleanly Restarting FinAgent Personal vs. Dev profile Profiles Every config key explained Configuration reference The category system and rule engine Categories &amp; rules","path":["FinAgent"],"tags":[]},{"location":"#the-short-version","level":2,"title":"The short version","text":"<ol> <li>Launch FinAgent. On first run the dashboard opens on the Settings page.</li> <li>Complete setup — choose your statements and processing folders, pick an    LLM provider and a location-lookup method, then press Complete setup.</li> <li>Import statements on the Import page. Use a CSV or supported tabular    text export from your bank — PDF statements are not supported.</li> <li>Process them on the Process page — a run button with live progress.</li> <li>Explore the Overview, Monthly, Transactions, and Budget pages.</li> </ol>","path":["FinAgent"],"tags":[]},{"location":"categories-rules/","level":1,"title":"Categories &amp; rules","text":"","path":["Categories &amp; rules"],"tags":[]},{"location":"categories-rules/#the-category-system","level":2,"title":"The category system","text":"<p>Categories are two-level: <code>group:subcategory</code> (e.g. <code>housing:rent_mortgage</code>). The canonical list lives in a reference CSV shipped with the app and is shown on the dashboard's Categories page with readable names and emojis.</p> <p>The set is deliberately closed: rules, location lookup, and the LLM prompt are all tuned against it. Adding categories is highly discouraged — it fragments your history and new categories only apply going forward.</p> <p>If you must add one (Dev profile only, via the Categories page):</p> <ul> <li>The group must be one of the existing groups.</li> <li>The subcategory is free-form; it is slugified to <code>lower_snake_case</code>.</li> <li>The reference CSV and the dynamic category store are both updated.</li> </ul>","path":["Categories &amp; rules"],"tags":[]},{"location":"categories-rules/#the-rule-engine","level":2,"title":"The rule engine","text":"<p>Rules run first in the pipeline — before location lookup and the LLM — and are deterministic keyword matches against transaction descriptions.</p> <p>Manage rules on the Rule Engine page:</p> <ul> <li>Create, edit, reorder (priority), and delete rules.</li> <li>Rules created from the dashboard are stored in <code>rules_dynamic.json</code> and   survive app updates.</li> <li>The rule suggester can propose rules from your uncategorized transactions.</li> </ul>","path":["Categories &amp; rules"],"tags":[]},{"location":"categories-rules/#editing-a-single-transaction","level":2,"title":"Editing a single transaction","text":"<p>Use the edit button on any row of the Transactions page. Manual edits are recorded with the <code>user_defined</code> stage (orange tag) and are preserved across processing runs.</p>","path":["Categories &amp; rules"],"tags":[]},{"location":"config-reference/","level":1,"title":"Configuration reference","text":"<p>Personal config lives at <code>~/.finagent/config.json</code> and Dev config normally lives at <code>~/.finagent-dev/config.json</code>. Override the active directory with the <code>FINAGENT_CONFIG_DIR</code> environment variable. Edit the selected profile through the dashboard's Settings page, the terminal wizard (<code>finagent --setup</code>), or by hand.</p>","path":["Configuration reference"],"tags":[]},{"location":"config-reference/#keys","level":2,"title":"Keys","text":"Key Values Notes <code>statements_dir</code> path Raw statements root. Locked after setup — rerun <code>finagent --setup</code> to change. <code>processing_dir</code> path Pipeline output + dashboard data. Locked after setup. <code>llm_provider</code> <code>ollama</code> · <code>open_router</code> · <code>claude</code> · <code>none</code> Categorization engine (setup guide). <code>llm_model</code> model name e.g. <code>llama3.1:8b</code> for Ollama. <code>ollama_model</code> model name Ollama only. Default <code>qwen2.5:3b</code>; must already be pulled (<code>ollama pull &lt;model&gt;</code>), e.g. <code>qwen3</code>. <code>ollama_api_url</code> URL Ollama only. Default <code>http://localhost:11434</code>. <code>openrouter_api_key</code> string Only for the OpenRouter provider. Free to generate at openrouter.ai/keys. <code>claude_api_key</code> string Only for the Claude provider. Paid — generate at console.anthropic.com after adding billing. <code>claude_model</code> model name Claude only. Default <code>claude-opus-4-5</code>. <code>location_lookup_method</code> <code>nominatim</code> · <code>none</code> <code>nominatim</code> is shown in the dashboard as \"Open Street Map\". <code>nominatim_email</code> email or <code>\"\"</code> Optional contact email shared with OpenStreetMap. Empty by default. <code>dashboard_port</code> number Default <code>3001</code>. Docs are served on port + 2. <code>ui_profile</code> <code>personal</code> · <code>dev</code> Profile recorded in this config (details). <code>setup_complete</code> bool Set automatically by first-run setup.","path":["Configuration reference"],"tags":[]},{"location":"config-reference/#ports","level":2,"title":"Ports","text":"Port Serves <code>dashboard_port</code> The dashboard (production) <code>dashboard_port + 1</code> API server (dev mode only) <code>dashboard_port + 2</code> This documentation site","path":["Configuration reference"],"tags":[]},{"location":"import/","level":1,"title":"Importing statements","text":"<p>The Import page accepts CSV or supported tabular text statement exports from your banks.</p> <p>PDF statements are not supported</p> <p>On your bank's download/export screen, choose CSV, comma-separated,    or a supported tabular text format. Do not download or upload a PDF: a PDF    is a visual document, not the transaction table FinAgent needs. Bank of    America checking <code>.txt</code> exports are converted to CSV automatically.</p>","path":["Importing statements"],"tags":[]},{"location":"import/#supported-banks","level":2,"title":"Supported banks","text":"Bank Folder Accounts American Express <code>AMEX</code> Credit Bank of America <code>BOA</code> Checking, Credit Chase <code>CHASE</code> Checking, Credit Citi <code>CITI</code> Credit US Bank <code>USBANK</code> Checking, Credit","path":["Importing statements"],"tags":[]},{"location":"import/#how-to-import","level":2,"title":"How to import","text":"<ol> <li>Download a CSV or supported tabular text export from your bank's website —    not a PDF.</li> <li>Drag it onto the Import page (or click to browse). FinAgent detects the    bank and account type from the file contents and files it into the right    folder under your statements directory.</li> <li>Repeat for as many statements as you like.</li> <li>Press Run processing — the button activates as soon as at least one    statement is present — to jump to the Process page.</li> </ol>","path":["Importing statements"],"tags":[]},{"location":"import/#importing-by-hand","level":2,"title":"Importing by hand","text":"<p>You can also drop files directly into the statements folders, e.g. <code>&lt;statements&gt;/BOA/CheckingAC/</code>. FinAgent picks them up on the next processing run.</p>","path":["Importing statements"],"tags":[]},{"location":"installation/","level":1,"title":"Installation","text":"<p>FinAgent ships as a standalone binary — no Python required for normal use. Pick your platform below.</p>","path":["Installation"],"tags":[]},{"location":"installation/#macos-quick-start","level":2,"title":"macOS quick start","text":"<ol> <li> <p>Install Node.js (required for the dashboard). FinAgent's dashboard is a    Node.js server that the app launches for you — it needs <code>node</code> available on    your <code>PATH</code>. Install the LTS release either way:</p> <ul> <li>Download the macOS installer from nodejs.org and    run it, or</li> <li>With Homebrew: <code>brew install node</code></li> </ul> <p>Verify it in a new Terminal window: <code>node --version</code> (v18 or newer; v20 LTS recommended).</p> </li> <li> <p>Download the latest <code>FinAgent-&lt;version&gt;-macOS.dmg</code> from the    GitHub releases page.</p> </li> <li> <p>Install: open the DMG and drag FinAgent.app into <code>Applications</code>.</p> </li> <li> <p>First launch — approve the security check. Development builds are not    notarized with a paid Apple Developer ID, so macOS Gatekeeper may say Apple    could not verify that FinAgent is free of malware and offer to move it to    the Trash. Approval is needed once per install/update. Ad-hoc signing    does not remove the warning for downloaded apps; avoiding it requires Apple    notarization and a paid Apple Developer account. Use one of these approval    methods:</p> <ul> <li>Right-click (or Control-click) FinAgent.app → Open → Open.</li> <li>If macOS still refuses (\"Apple could not verify…\"), go to    System Settings → Privacy &amp; Security, scroll to the Security    section, and click \"Open Anyway\" next to the FinAgent message,    then confirm with your password or Touch ID.</li> <li>For a release downloaded from the official FinAgent Releases page that    you trust, Terminal can remove the quarantine flag with    <code>xattr -dr com.apple.quarantine /Applications/FinAgent.app</code>.</li> </ul> </li> <li> <p>Launch. Double-clicking FinAgent.app opens a Terminal window, starts the    app, and opens your browser on the dashboard. First run walks you through    setup.</p> </li> </ol>","path":["Installation"],"tags":[]},{"location":"installation/#set-up-the-finagent-terminal-alias-macos","level":3,"title":"Set up the <code>finagent</code> terminal alias (macOS)","text":"<p>The docs and dashboard refer to the <code>finagent</code> command. The binary lives inside the app bundle, so add an alias to your shell profile:</p> <pre><code># zsh (default on modern macOS)\necho 'alias finagent=\"/Applications/FinAgent.app/Contents/MacOS/finagent\"' &gt;&gt; ~/.zshrc\nsource ~/.zshrc\n\n# bash\necho 'alias finagent=\"/Applications/FinAgent.app/Contents/MacOS/finagent\"' &gt;&gt; ~/.bash_profile\nsource ~/.bash_profile\n</code></pre> <p>Verify it works:</p> <pre><code>finagent --version\n</code></pre> <p>The wrapper at <code>Contents/MacOS/finagent</code> detects how it was invoked: from a shell it runs inline (all terminal flags work as expected); from Finder it opens Terminal.app for you.</p>","path":["Installation"],"tags":[]},{"location":"installation/#linux-quick-start","level":2,"title":"Linux quick start","text":"<ol> <li> <p>Install Node.js (required for the dashboard). FinAgent launches a    Node.js server for the dashboard, so <code>node</code> must be on your <code>PATH</code> first:</p> <pre><code># Debian/Ubuntu — NodeSource LTS repo (gives a current version; the\n# distro-packaged \"nodejs\" apt package is often too old)\ncurl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -\nsudo apt-get install -y nodejs\n\n# Fedora/RHEL\nsudo dnf install -y nodejs\n\n# Arch\nsudo pacman -S nodejs npm\n</code></pre> <p>Verify with <code>node --version</code> (v18+; v20 LTS recommended). 2. Download the Linux archive from the releases page and extract it:</p> <pre><code>tar -xzf FinAgent-&lt;version&gt;-linux.tar.gz -C ~/finagent\n</code></pre> </li> <li> <p>Make the command available on your PATH (pick one):</p> <pre><code># symlink (recommended)\nsudo ln -s ~/finagent/finagent /usr/local/bin/finagent\n\n# or shell alias\necho 'alias finagent=\"$HOME/finagent/finagent\"' &gt;&gt; ~/.bashrc &amp;&amp; source ~/.bashrc\n</code></pre> </li> <li> <p>Run <code>finagent</code> — the dashboard opens in your default browser. If your    distribution blocks the binary, mark it executable first:    <code>chmod +x ~/finagent/finagent</code>.</p> </li> </ol>","path":["Installation"],"tags":[]},{"location":"installation/#windows-quick-start","level":2,"title":"Windows quick start","text":"<ol> <li> <p>Install Node.js first — this is required before FinAgent can start the    dashboard. FinAgent's dashboard runs as a Node.js server; the app looks    for <code>node</code> on your <code>PATH</code> and will not start the dashboard without it.</p> <ul> <li>Download the LTS installer from nodejs.org   (the \"Windows Installer (.msi)\" button).</li> <li>Run the installer and click through with the defaults (Next → Next →   Install) — this also adds Node to your <code>PATH</code> automatically.</li> <li> <p>Open a new PowerShell or Command Prompt window (required so it picks   up the updated <code>PATH</code>) and confirm:</p> <pre><code>node --version\n</code></pre> </li> </ul> <p>You should see <code>v18</code> or newer (<code>v20.x</code> LTS recommended). 2. Download the Windows <code>.zip</code> from the releases page and extract it, e.g. to    <code>C:\\Program Files\\FinAgent</code>. 3. SmartScreen approval (first run only): double-click <code>finagent.exe</code> —    when \"Windows protected your PC\" appears, click More info → Run anyway. 4. Optional — add FinAgent to your PATH so <code>finagent</code> works in PowerShell:    Settings → System → About → Advanced system settings → Environment    Variables, edit <code>Path</code>, and add the FinAgent folder. Then:</p> <pre><code>finagent --version\n</code></pre> </li> </ol>","path":["Installation"],"tags":[]},{"location":"installation/#requirements","level":2,"title":"Requirements","text":"<ul> <li>Node.js 18+ (20 LTS recommended) — required on every platform. FinAgent   starts a small Node.js server for the dashboard and looks for <code>node</code> on your   <code>PATH</code>; without it the app still runs the categorization pipeline, but the   dashboard won't launch. See the Node.js step in the quick start above for   your platform.</li> <li>A categorization engine — pick one: Ollama (local, free, private —   recommended), OpenRouter (cloud API, free to sign up), or Claude   (cloud API, paid). Full setup steps for each are below.</li> <li>~500 MB disk for the app plus space for your processed data (and a few GB   more if you run a local Ollama model).</li> </ul>","path":["Installation"],"tags":[]},{"location":"installation/#setting-up-an-llm-provider","level":2,"title":"Setting up an LLM provider","text":"<p>FinAgent uses an LLM to categorize transactions its rule engine can't classify on its own. You choose the provider during first-run setup (or later on the Settings page / <code>finagent --setup</code>). Pick one of the three:</p>","path":["Installation"],"tags":[]},{"location":"installation/#option-a-ollama-local-free-private-recommended","level":3,"title":"Option A — Ollama (local, free, private, recommended)","text":"<p>Runs entirely on your machine — transaction descriptions never leave your computer, and there's no API key or bill.</p> <ol> <li> <p>Install Ollama:</p> <ul> <li>macOS: download from ollama.com/download   and drag it to Applications, or <code>brew install ollama</code>.</li> <li>Windows: download the installer from   ollama.com/download and run it. Ollama   installs itself as a background service with a system-tray icon.</li> <li> <p>Linux:</p> <pre><code>curl -fsSL https://ollama.com/install.sh | sh\n</code></pre> </li> </ul> <p>Ollama starts automatically after install and listens on <code>http://localhost:11434</code>. Verify with:</p> <pre><code>ollama --version\n</code></pre> </li> <li> <p>Pull a default model. FinAgent's built-in default is the small    <code>qwen2.5:3b</code>, but the newer Qwen 3 family is a great free default —    fast, accurate enough for categorization, and light on RAM:</p> <pre><code>ollama pull qwen3\n</code></pre> <p>On a machine with limited RAM (≤8 GB), use the smaller tag instead:</p> <pre><code>ollama pull qwen3:4b\n</code></pre> </li> <li> <p>Point FinAgent at the model. In FinAgent's Settings page, choose    Ollama as the LLM backend and set the Ollama model field to the    tag you pulled (e.g. <code>qwen3</code> or <code>qwen3:4b</code>). From the terminal wizard, the    same prompt is called \"Ollama model name\".</p> </li> <li> <p>Verify it works:</p> <pre><code>ollama list          # confirms the model was downloaded\nollama run qwen3 \"hi\" # quick smoke test\n</code></pre> </li> </ol>","path":["Installation"],"tags":[]},{"location":"installation/#option-b-openrouter-cloud-api-free-to-get-a-key","level":3,"title":"Option B — OpenRouter (cloud API — free to get a key)","text":"<p>OpenRouter gives you access to many hosted models through one API. Creating an account and generating an API key costs nothing, and OpenRouter offers several <code>:free</code>-tagged models you can use at no charge (paid models are billed per-token only if you choose one later).</p> <ol> <li>Go to openrouter.ai and click Sign Up (GitHub,    Google, or email — free).</li> <li>Once logged in, open openrouter.ai/keys.</li> <li>Click Create Key, give it a name (e.g. \"FinAgent\"), and click Create.</li> <li>Copy the key immediately — it's shown only once and starts with    <code>sk-or-...</code>.</li> <li>In FinAgent, choose OpenRouter as the LLM backend and paste the key    into the OpenRouter API key field (Settings page, or the    <code>finagent --setup</code> wizard).</li> </ol>","path":["Installation"],"tags":[]},{"location":"installation/#option-c-claude-anthropic-cloud-api-paid","level":3,"title":"Option C — Claude / Anthropic (cloud API — paid)","text":"<p>Anthropic's Claude models give the highest categorization accuracy but require a billed API key — unlike OpenRouter, there is no free tier.</p> <ol> <li>Go to console.anthropic.com and sign up    or log in.</li> <li>Add billing (required before the API will work): open    Settings → Billing, add a payment method, and purchase API credits.    Anthropic bills per token — check current rates at    anthropic.com/pricing before heavy use.</li> <li>Open API Keys in the console sidebar and click Create Key. Name it    (e.g. \"FinAgent\") and create it.</li> <li>Copy the key immediately — it's shown only once and starts with    <code>sk-ant-...</code>.</li> <li>In FinAgent, choose Claude as the LLM backend and paste the key into    the Claude API key field. The default model is <code>claude-opus-4-5</code>; you    can change it in the same form.</li> </ol>","path":["Installation"],"tags":[]},{"location":"installation/#where-finagent-stores-things","level":2,"title":"Where FinAgent stores things","text":"Path Purpose <code>~/.finagent/config.json</code> Your configuration (reference) Statements folder (you choose) Raw bank CSV/tabular statement exports (not PDFs) Processing folder (you choose) Pipeline output + dashboard data","path":["Installation"],"tags":[]},{"location":"processing/","level":1,"title":"Processing pipeline","text":"<p>The Process page runs the full FinAgent pipeline: statement parsing → rule-based categorization → location lookup → LLM categorization → merge → dashboard data generation.</p>","path":["Processing pipeline"],"tags":[]},{"location":"processing/#running-from-the-dashboard","level":2,"title":"Running from the dashboard","text":"<ul> <li>No dates selected (default): processes everything available.</li> <li>Date range: once a first run exists, the page shows the date range your   data covers and lets you narrow a run with start/end date pickers.</li> <li>Live progress: an overall progress bar plus sub-bars for   location lookup and LLM categorization, with a collapsible run log.</li> </ul> <p>Run defaults (LLM provider, lookup method) come from Settings — change them there, not on the Process page.</p>","path":["Processing pipeline"],"tags":[]},{"location":"processing/#advanced-switches-terminal-only","level":2,"title":"Advanced switches (terminal only)","text":"<p>The remaining pipeline options are deliberately terminal-only features:</p> Flag Purpose <code>--orgs boa,amex</code> Limit the run to specific banks <code>--month 2026-05</code> Process a single month <code>--clean-data</code> Delete generated temp/transaction data first <code>--no-location-lookup</code> Skip the location stage entirely <code>--lookup-method nominatim\\|none</code> Override lookup method for one run <code>--clear-lookup-cache</code> Wipe the lookup cache before running <code>--refresh-lookup-cache</code> Ignore cached lookups, overwrite with fresh results <p>See Running from the terminal for the full command set.</p>","path":["Processing pipeline"],"tags":[]},{"location":"processing/#categorization-stages","level":2,"title":"Categorization stages","text":"<p>Every transaction records how it was categorized in the <code>categorization_stage</code> column — and the Transactions page colors category tags accordingly:</p> Stage Meaning Color <code>rule</code> Deterministic keyword rule Blue <code>location_lookup</code> Resolved from merchant location data Teal <code>llm:&lt;provider&gt;</code> Categorized by the language model Violet <code>user_defined</code> Edited by you in the dashboard Orange <code>merge</code> Carried over from a previous run Gray <code>uncategorized</code> Nothing matched Red","path":["Processing pipeline"],"tags":[]},{"location":"profiles/","level":1,"title":"Profiles: Personal &amp; Dev","text":"<p>The dashboard has a profile toggle (top-right of the Settings page) with two modes. Personal is the default and what almost everyone should use.</p>","path":["Profiles: Personal &amp; Dev"],"tags":[]},{"location":"profiles/#personal-default","level":2,"title":"Personal (default)","text":"<p>Personal keeps the dashboard focused and privacy-first:</p> <ul> <li>Location lookup offers Open Street Map (free, built in) or off.</li> <li>An optional checkbox lets you share a contact email with OpenStreetMap —   purely voluntary feedback, off by default.</li> <li>Advanced/experimental lookup integrations are hidden.</li> <li>Category editing is limited to what the pipeline supports safely.</li> </ul>","path":["Profiles: Personal &amp; Dev"],"tags":[]},{"location":"profiles/#dev","level":2,"title":"Dev","text":"<p>Dev mode unlocks maintainer features:</p> <ul> <li>A separate development configuration that stays isolated from your personal data.</li> <li>The Add category button on the Categories page (still discouraged —   the category set is tuned for the rule engine and LLM prompts).</li> </ul>","path":["Profiles: Personal &amp; Dev"],"tags":[]},{"location":"profiles/#activating-dev","level":3,"title":"Activating Dev","text":"<p>Flip the toggle to Dev on the Settings page and enter the password (<code>finagent</code>). FinAgent then loads the Dev configuration into the Settings form and shows its config-file path. This is only a preview until you press Save changes:</p> <ul> <li>Save changes writes the displayed profile's settings and makes that   profile active across the dashboard and subsequent Process runs.</li> <li>Discard returns to the currently active profile without changing its   configuration.</li> <li>Switching back to Personal does not require a password, but it still waits   for Save changes before activation.</li> </ul> <p>Personal normally uses <code>~/.finagent/config.json</code>; Dev normally uses <code>~/.finagent-dev/config.json</code>. Each file retains its own folders, provider, API keys, and processing settings.</p>","path":["Profiles: Personal &amp; Dev"],"tags":[]},{"location":"profiles/#terminal-config-profiles","level":2,"title":"Terminal config profiles","text":"<p>From the terminal, keep to the same two supported profiles used by the dashboard:</p> <pre><code>finagent --profile personal\nfinagent --profile dev\n</code></pre> <p>Each profile keeps its own folders, provider, and settings. See Running from the terminal.</p> <p>The profile supplied at launch is initially active. Running <code>finagent</code> without <code>--profile</code> starts Personal; use <code>--profile dev</code> when Dev should be active immediately.</p>","path":["Profiles: Personal &amp; Dev"],"tags":[]},{"location":"profiles/#development-restart-helper","level":2,"title":"Development restart helper","text":"<p>When <code>dashboard/restart-server.sh</code> starts with <code>--profile</code> and <code>--config-dir</code>, that custom config directory remains associated with the starting profile. Settings edits that exact file while the profile is selected. The other profile continues to use its normal config path.</p> <p>An explicit <code>--data-dir</code> is a pinned development override and remains in use when profiles change. Without that override, activating a profile also changes the dashboard data and statements paths to those in the selected config.</p>","path":["Profiles: Personal &amp; Dev"],"tags":[]},{"location":"restart/","level":1,"title":"Restarting FinAgent","text":"<p>Some Settings changes (dashboard port, folders) only take effect after a restart.</p>","path":["Restarting FinAgent"],"tags":[]},{"location":"restart/#if-you-launched-the-app-dmg-binary","level":2,"title":"If you launched the app (DMG / binary)","text":"<ol> <li>Quit: close the terminal window FinAgent opened, or press    ++ctrl+c++ in it. On macOS you can also quit the FinAgent app from the Dock.</li> <li>Relaunch the app. The dashboard reopens in your browser on the    configured port.</li> </ol>","path":["Restarting FinAgent"],"tags":[]},{"location":"restart/#if-you-launched-from-the-terminal","level":2,"title":"If you launched from the terminal","text":"<pre><code># stop\nCtrl-C\n\n# start again (skip reprocessing)\nfinagent --dashboard-only\n</code></pre>","path":["Restarting FinAgent"],"tags":[]},{"location":"restart/#troubleshooting","level":2,"title":"Troubleshooting","text":"<ul> <li> <p>Port already in use — an old FinAgent process is still running:</p> <pre><code>lsof -ti tcp:3000 | xargs kill    # replace 3000 with your port\n</code></pre> </li> <li> <p>Dashboard shows stale data — hard-refresh the browser   (++cmd+shift+r++ on macOS, ++ctrl+shift+r++ elsewhere).</p> </li> <li>Changed folders in Settings — folders are locked after first-run setup;   re-run <code>finagent --setup</code> from the terminal to move them safely.</li> </ul>","path":["Restarting FinAgent"],"tags":[]},{"location":"setup/","level":1,"title":"First-run setup","text":"<p>The very first time you launch FinAgent (no <code>~/.finagent/config.json</code> yet), the app starts the dashboard and opens the Settings page in setup mode. Every other page stays locked until setup is complete.</p>","path":["First-run setup"],"tags":[]},{"location":"setup/#the-three-steps","level":2,"title":"The three steps","text":"<ol> <li>Folders — choose where your raw statements live and where processed    data should go. FinAgent creates the bank subfolders    (<code>AMEX</code>, <code>BOA</code>, <code>CHASE</code>, <code>CITI</code>, <code>USBANK</code> — each with <code>CheckingAC</code> and    <code>CreditAC</code>) automatically when you complete setup.</li> <li>Categorization engine — pick an LLM provider (Ollama, OpenRouter, or    Claude) and a location-lookup method. See    Setting up an LLM provider    for step-by-step instructions, including how to get a free OpenRouter key    or a paid Claude key, and pulling a default Ollama model.</li> <li>Dashboard — pick the port the dashboard runs on.</li> </ol> <p>Press Complete setup. The Import page unlocks immediately; the remaining pages unlock after your first processing run generates dashboard data. Import a CSV or supported tabular text export downloaded from your bank; PDF statements are not supported.</p>","path":["First-run setup"],"tags":[]},{"location":"setup/#how-pages-unlock","level":2,"title":"How pages unlock","text":"State Unlocked pages No config yet Settings only Setup complete, no statements Settings, Import Statements imported, no dashboard data Settings, Import, Process Dashboard data exists Everything","path":["First-run setup"],"tags":[]},{"location":"setup/#prefer-the-terminal","level":2,"title":"Prefer the terminal?","text":"<p>The classic terminal wizard is still there:</p> <pre><code>finagent --setup\n</code></pre> <p>See Running from the terminal for all flags.</p>","path":["First-run setup"],"tags":[]},{"location":"terminal/","level":1,"title":"Running from the terminal","text":"<p>Everything the dashboard does — and more — is available from the terminal. Use the <code>finagent</code> command after installation.</p>","path":["Running from the terminal"],"tags":[]},{"location":"terminal/#everyday-commands","level":2,"title":"Everyday commands","text":"<pre><code>finagent                    # run pipeline, then serve the dashboard\nfinagent --dashboard-only   # skip processing, just serve the dashboard\nfinagent --pipeline-only    # process data, don't start the dashboard\nfinagent --setup            # (re)run the interactive setup wizard\nfinagent --version          # print the version\nfinagent --help             # full flag reference\n</code></pre>","path":["Running from the terminal"],"tags":[]},{"location":"terminal/#pipeline-switches","level":2,"title":"Pipeline switches","text":"<pre><code>finagent --orgs boa,amex            # only these banks\nfinagent --month 2026-05            # single month\nfinagent --start-date 2026-01-01 --end-date 2026-06-30\nfinagent --clean-data               # wipe generated temp data first\nfinagent --no-location-lookup       # rules + LLM only\nfinagent --lookup-method nominatim  # one-run lookup override\nfinagent --clear-lookup-cache       # wipe the lookup cache\nfinagent --refresh-lookup-cache     # re-resolve every location\n</code></pre> <p>If no statements are present, a terminal run starts the dashboard and opens its Import page. Import a CSV or supported tabular text bank export there; PDF statements are not supported. The old desktop import window is no longer used.</p> <p>Cloud API keys from the terminal</p> <p>Keys entered in FinAgent Settings or <code>finagent --setup</code> are saved in your profile, so you do not need to export them again. If you select a cloud provider only with terminal options and have not saved its key, set the matching environment variable before starting FinAgent. The pipeline and the dashboard server inherit it.</p> macOS / LinuxWindows PowerShell <pre><code>export OPENROUTER_API_KEY=\"sk-or-your-key\"\nfinagent --llm-provider open_router --orgs boa\n\nexport ANTHROPIC_API_KEY=\"sk-ant-your-key\"\nfinagent --llm-provider claude --orgs boa\n</code></pre> <pre><code>$env:OPENROUTER_API_KEY = \"sk-or-your-key\"\nfinagent --llm-provider open_router --orgs boa\n\n$env:ANTHROPIC_API_KEY = \"sk-ant-your-key\"\nfinagent --llm-provider claude --orgs boa\n</code></pre> <p>These commands set the key only for the current terminal session. Avoid putting API keys directly in commands that may be saved in shell history.</p>","path":["Running from the terminal"],"tags":[]},{"location":"terminal/#maintenance","level":2,"title":"Maintenance","text":"<pre><code>finagent --overwrite-dashboard-data   # regenerate dashboard data from scratch\nfinagent --delete-dashboard-data      # remove dashboard data (asks first)\nfinagent --reset-all                  # factory reset config + data (asks first)\nfinagent --reset-all --with-statements  # ...including raw statements\nfinagent --yes                        # answer \"yes\" to confirmation prompts\n</code></pre>","path":["Running from the terminal"],"tags":[]},{"location":"terminal/#profiles-from-the-terminal","level":2,"title":"Profiles from the terminal","text":"<pre><code>finagent --profile personal   # run with the personal profile\nfinagent --profile dev        # run with the dev profile\n</code></pre> <p>Only these two profiles are supported in the user-facing workflow right now. See Profiles for what the dashboard's Personal/Dev toggle does.</p>","path":["Running from the terminal"],"tags":[]},{"location":"terminal/#dashboard-view-range","level":2,"title":"Dashboard view range","text":"<pre><code>finagent --dashboard-only --start-date 2026-01-01 --end-date 2026-03-31\n</code></pre> <p>Serves the dashboard restricted to that date window without reprocessing.</p>","path":["Running from the terminal"],"tags":[]}]}